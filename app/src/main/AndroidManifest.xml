<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinube.bonpreu">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity"/>
        <activity android:name=".saltedge.SaltEdgeDashboardActivity"/>


        <activity android:name=".CustomerCreateActivity"/>
        <activity android:name=".SelectionActivity"/>
        <activity android:name=".saltedge.SaltEdgePaymentSelectionActivity"/>
        <activity android:name=".saltedge.SaltEdgeCredentialsPaymentActivity"/>
        <activity android:name=".saltedge.SaltEdgeDirectPayActivity"/>
        <activity android:name=".RegistrationActivity">

        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".afterbank.AfterbankDashboard" />
    </application>

</manifest>